alias +c4 slot5;
alias -c4 drop;
bind n +c4;

bind x "voluem 1"
bind c "voluem 0.3"

alias "+jumpaction" "+jump;"
alias "+throwaction" "-attack; -attack2"
alias "-jumpaction" "-jump"
bind n "+jumpaction;+throwaction;"

bind "MWHEELDOWN" "+jump";
bind "MWHEELUP" "+jump";
bind "SPACE" "+jump"

alias +knife slot3
alias -knife lastinv
bind q +knife

bind p "bot_place"

bind z "noclip"

bind RSHIST "sv_cheats true"

r_show_build_info false

bind "`" "toggleconsole"

cl_updaterate 256
cl_interp_ratio 1
cl_interp 0.015625

